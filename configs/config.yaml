seed: 42

data:
  root: "data"
  dataset_name: "ESOL"
  train_ratio: 0.8
  val_ratio: 0.1

model:
  hidden_dim: 128
  num_layers: 2
  dropout: 0.2

training:
  batch_size: 16
  lr: 0.003
  epochs: 200
  weight_decay: 0.0001
